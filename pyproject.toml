[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "embedapi"
version = "0.0.8"
dependencies = [
    "requests",
    "numpy",
    "more_itertools",
    "openai",
]
