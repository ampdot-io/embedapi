[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "embedapi"
version = "0.0.2"
dependencies = [
    "requests",
    "numpy",
    "more_itertools",
    "openai",
]
